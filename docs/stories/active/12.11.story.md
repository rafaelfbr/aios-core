# Story 12.11: CI/CD Pipeline for Bob

story_id: "12.11"
epic_id: "12"
epic_title: "Bob Full Integration — Completando o PRD v2.0"
title: "CI/CD Pipeline for Bob"
status: "Ready for Review"
priority: "P1-HIGH"
created_at: "2026-02-05"
author: "@sm (River)"

executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [code_review, architecture_validation]

blocked_by: [] # 12.3, 12.9, 12.10 Done — desbloqueada 2026-02-05
blocks: []

---

## Story Statement

**As a** AIOS maintainer,
**I want** automated CI/CD pipelines for Bob,
**So that** quality is validated automatically and releases are streamlined.

---

## Acceptance Criteria

> **NOTA ARQUITETURAL (Estado Atual do Código):**
>
> **Já implementado (.github/workflows/):**
> - `ci.yml` — CI consolidado com path-based change detection (dorny/paths-filter), jobs: Detect Changes, Lint, Tests, Build. Roda em push to main e PRs to main.
> - `test.yml` — Security audit (`npm audit`) e penetration test (push to main only)
> - `release.yml` — Release automation triggered by git tags `v*`, cria GitHub Release
> - `semantic-release.yml` — Semantic versioning automatizado
> - `npm-publish.yml` — NPM package publishing (já existe, mas para aios-core — não para Bob)
> - `macos-testing.yml` — Cross-platform macOS testing
> - `pr-automation.yml`, `pr-labeling.yml`, `welcome.yml` — PR utilities
> - `quarterly-gap-audit.yml` — Periodic audits
>
> **Já implementado (package.json scripts):**
> - `npm test` (Jest), `npm run lint` (ESLint), `npm run typecheck` (tsc --noEmit)
> - `npm run test:coverage`, `npm run test:health-check` (Mocha)
> - `npm run release` (semantic-release)
> - `npm run validate:manifest`, `npm run validate:structure`, `npm run validate:agents`
>
> **O delta desta story:**
> 1. Criar workflow `bob-integration.yml` — valida bob-orchestrator em cada PR
> 2. Criar workflow `cross-platform.yml` — testa pm.sh em matrix (ubuntu, macos, windows)
> 3. Atualizar `npm-publish.yml` para incluir `@synkra/aios-install` (de 12.9)
> 4. Configurar CodeRabbit review automático para `.aios-core/core/`
> 5. Configurar branch protection com required status checks
> 6. Criar release workflow com approval manual

**GitHub Actions Workflows:**
- [x] AC1: Workflow `bob-integration.yml` valida bob-orchestrator.js em cada PR — roda testes de bob-orchestrator, workflow-executor, terminal-spawner
- [x] AC2: Workflow `npm-publish.yml` atualizado para publicar `@synkra/aios-install` on release tag `v*.*.*`
- [x] AC3: Workflow `cross-platform.yml` testa pm.sh em matrix (ubuntu-latest, macos-latest, windows-latest) com fallback inline

**Quality Gates:**
- [x] AC4: CodeRabbit review automático em PRs que tocam `.aios-core/core/` — configurar via `.coderabbit.yaml` ou workflow annotation
- [ ] AC5: Required status checks para merge em main: lint, typecheck, test, bob-integration — DEFERRED (Task 5 por último, conforme PO)

**Release Automation:**
- [x] AC6: Release notes geradas automaticamente via conventional commits (já parcialmente em `semantic-release.yml`)
- [x] AC7: npm publish triggered por tag `v*.*.*` com approval manual (environment protection rule) — já implementado em npm-publish.yml
- [x] AC8: Smoke test pós-publish: `npx @synkra/aios-install --version` valida package publicado — já implementado em npm-publish.yml

**Branch Protection:**
- [ ] AC9: Branch `main` protegida com required reviews (≥1) e status checks — DEFERRED (aplicar após merge de stories pendentes)
- [ ] AC10: Squash merge enforced para histórico limpo — DEFERRED (aplicar após merge de stories pendentes)

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type**: Deployment/Infrastructure
- **Secondary Type(s)**: None
- **Complexity**: Medium (GitHub Actions YAML, branch protection config, workflow orchestration)

**Specialized Agent Assignment:**
- **Primary Agents**:
  - @devops (pre-commit reviews, CI/CD configuration)
  - @architect (architecture review — pipeline design, quality gate strategy)

- **Supporting Agents**: None

**Quality Gate Tasks:**
- [ ] Pre-Commit (@devops): Run `coderabbit --prompt-only -t uncommitted` before marking story complete
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` before creating pull request

**CodeRabbit Focus Areas:**
- **Primary Focus**:
  - CI/CD: workflow YAML syntax correto, jobs dependencies, matrix strategy
  - Secrets management: `NPM_TOKEN`, `GITHUB_TOKEN` — via GitHub Secrets, nunca hardcoded
  - Environment config: Node.js version consistent (18+), proper caching (actions/cache)

- **Secondary Focus**:
  - Rollback readiness: npm unpublish possível dentro de 72h se publicação falhar
  - Concurrency: workflows não cancelam runs in-progress para releases
  - Branch protection: regras aplicadas corretamente via gh CLI ou GitHub API

**Self-Healing Configuration:**
- **Expected Self-Healing**:
  - Primary Agent: @devops (check mode)
  - Max Iterations: 0
  - Timeout: N/A
  - Severity Filter: report_only

- **Predicted Behavior**:
  - CRITICAL issues: report_only
  - HIGH issues: report_only

---

## Tasks / Subtasks

- [x] Task 1 (AC: 1): Criar workflow bob-integration.yml
  - [x] 1.1: Criar `.github/workflows/bob-integration.yml` — trigger: `pull_request` para `main`
  - [x] 1.2: Path filter: apenas quando arquivos em `.aios-core/core/orchestration/`, `.aios-core/core/events/`, `tests/core/orchestration/` são modificados
  - [x] 1.3: Job `bob-test`: checkout → setup-node@v4 (Node 20) → npm ci → rodar testes específicos:
    - `npx jest tests/core/orchestration/bob-orchestrator.test.js`
    - `npx jest tests/core/orchestration/workflow-executor-callbacks.test.js` (nota: workflow-executor.test.js não existe, usado callbacks variant)
    - `npx jest tests/core/orchestration/terminal-spawner.test.js`
    - `npx jest tests/core/orchestration/lock-manager.test.js`
    - Plus: session-state, message-formatter, bob-status-writer, brownfield-handler, greenfield-handler
  - [x] 1.4: Job `bob-lint`: lint apenas arquivos de orchestration e events
  - [x] 1.5: Cache npm dependencies via setup-node cache: 'npm' (padrão ci.yml)

- [x] Task 2 (AC: 3): Criar workflow cross-platform-bob.yml
  - [x] 2.1: Criar `.github/workflows/cross-platform-bob.yml` — trigger: `pull_request` para `main`
  - [x] 2.2: Path filter: quando `.aios-core/scripts/pm.sh` ou `.aios-core/core/orchestration/terminal-spawner.js` ou testes são modificados
  - [x] 2.3: Matrix strategy: `os: [ubuntu-latest, macos-latest, windows-latest]`
  - [x] 2.4: Job steps: checkout → setup-node → npm ci → executar testes de terminal-spawner
  - [x] 2.5: Em Windows, testar via PowerShell fallback inline mode
  - [x] 2.6: Annotation com resultado por OS via `::notice` annotation

- [x] Task 3 (AC: 2, 7, 8): Atualizar npm-publish.yml — JÁ IMPLEMENTADO (Story 12.9)
  - [x] 3.1: `publish-aios-install` job já existe em npm-publish.yml
  - [x] 3.2: Trigger: tag `v*.*.*` + workflow_dispatch + release event
  - [x] 3.3: Steps completos com checkout, setup-node, npm ci, npm publish --access public
  - [ ] 3.4: Environment protection: `npm-publish` environment com required reviewer — DEFERRED (GitHub Settings, aplicar pós-merge)
  - [x] 3.5: Smoke test pós-publish com retry 6x, delay 15s
  - [x] 3.6: Concurrency: `group: npm-publish-${{ github.ref }}`, `cancel-in-progress: false`

- [x] Task 4 (AC: 4): Configurar CodeRabbit review automático
  - [x] 4.1: Atualizado `.coderabbit.yaml` na raiz do repositório
  - [x] 4.2: Adicionado path-based reviews para `.aios-core/core/**` e `.aios-core/core/orchestration/**`
  - [x] 4.3: CodeRabbit auto_review enabled para PRs contra main (já configurado)
  - [x] 4.4: CodeRabbit GitHub App — verificação pendente no GitHub (pré-existente desde COLLAB-1)

- [ ] Task 5 (AC: 5, 9, 10): Configurar branch protection — DELEGADA a @devops (Gage)
  - [x] 5.1: Via `gh api` ou GitHub Settings UI, configurar branch protection para `main` — ✅ PRÉ-EXISTENTE (verificado 2026-02-05)
  - [ ] 5.2: Adicionar `bob-test` aos required status checks — EXECUTAR PÓS-MERGE de feat/epic-12
  - [x] 5.3: Required reviews: ≥1 approving review — ✅ PRÉ-EXISTENTE (1 review, dismiss stale, code owner required)
  - [ ] 5.4: Verificar squash merge enforced via GitHub Settings > General > Pull Requests
  - [ ] 5.5: Documentar configuração aplicada em PR description
  - **PO Notes (2026-02-05):** Branch protection 80% pré-configurada. Delta: adicionar bob-test + verificar squash merge. EXECUTAR APÓS merge feat/epic-12 → main para evitar bloqueio de PRs.

- [x] Task 6 (AC: 6): Configurar release notes automáticas
  - [x] 6.1: Verificado `semantic-release.yml` — funcional com workflow_dispatch
  - [x] 6.2: Atualizado `.releaserc.json` com preset conventionalcommits e categorização completa
  - [x] 6.3: Release notes categorizadas: Features, Bug Fixes, Performance, Reverts, Documentation, Maintenance, Refactoring
  - [x] 6.4: Adicionado @semantic-release/changelog (CHANGELOG.md) e @semantic-release/git (commit back)

- [x] Task 7: Testes e validação (parcial — completa após merge)
  - [x] 7.1: Validação local: YAML syntax check passed para todos os workflows novos
  - [x] 7.2: cross-platform-bob.yml matrix configurada para 3 OS — validação CI via PR
  - [x] 7.3: npm-publish.yml já tem smoke test e dry-run não é necessário (já em produção)
  - [ ] 7.4: Verificar que branch protection bloqueia merge — DEFERRED (Task 5)
  - [ ] 7.5: Verificar que squash merge está enforced — DEFERRED (Task 5)
  - [x] 7.6: Documentação de workflows no story Dev Agent Record

- [x] Task 8: Integração final
  - [x] 8.1: Full test suite: lint (0 errors, 29 warnings), typecheck (pass), bob tests (316 tests pass)
  - [x] 8.2: Todos 8 workflows existentes validados — YAML syntax OK, nenhum quebrado
  - [x] 8.3: Documentação em Dev Agent Record (abaixo)

---

## Dev Notes

### PRD Reference
- Implícito em §9 (Instalação deve ser confiável — CI valida isso)
- §3.4 (Mecanismo de Orquestração — testes automatizados)

### Previous Story Insights

**Story 12.3 (Bob Orchestration Logic):**
- `bob-orchestrator.js` com 35 testes passando
- `lock-manager.js` com 17 testes passando
- Testes em `tests/core/orchestration/bob-orchestrator.test.js`
- Pattern de testes: Jest mocks, Given-When-Then

[Source: 12.3.story.md — Dev Agent Record]

**Story 12.10 (Terminal Spawning E2E):**
- `terminal-spawner.js` com 43 testes passando
- pm.sh testado em macOS — precisa CI validation para Linux e Windows
- Fallback inline funciona para ambientes sem terminal (CI, Docker, SSH)

[Source: 12.10.story.md — Dev Agent Record]

**Story 12.9 (NPX Installer):**
- `packages/aios-install/` criado com npm publish workflow
- npm-publish.yml referenciado como dependência

[Source: 12.9.story.md]

### Código Existente a Reutilizar

**`.github/workflows/ci.yml`** — CI consolidado existente
- Path-based change detection via `dorny/paths-filter@v3`
- Jobs: detect-changes → lint (condicional) → tests (condicional) → build (condicional)
- Node.js 20, npm ci, actions/cache
- Pattern de referência para novos workflows

[Source: `.github/workflows/ci.yml`]

**`.github/workflows/release.yml`** — Release existente
- Triggered by tags `v*`
- Cria GitHub Release com release notes
- Concurrency: `cancel-in-progress: false` para releases
- Pattern para approval manual

[Source: `.github/workflows/release.yml`]

**`.github/workflows/npm-publish.yml`** — NPM publish existente
- Publish para npm registry
- Referência para estrutura e secrets handling

[Source: `.github/workflows/npm-publish.yml`]

**`.github/workflows/semantic-release.yml`** — Semantic versioning
- Gera versão e release notes automaticamente
- Baseado em conventional commits

[Source: `.github/workflows/semantic-release.yml`]

**`package.json`** — Scripts de quality gate
```json
{
  "test": "jest",
  "lint": "eslint . --cache --cache-location .eslintcache",
  "typecheck": "tsc --noEmit",
  "test:coverage": "jest --coverage",
  "release": "semantic-release"
}
```

[Source: `package.json`]

### Workflows a Criar/Atualizar

```
.github/workflows/
├── ci.yml                    # EXISTENTE — não modificar
├── test.yml                  # EXISTENTE — não modificar
├── release.yml               # EXISTENTE — verificar integração
├── semantic-release.yml      # EXISTENTE — verificar categorias
├── npm-publish.yml           # ATUALIZAR — adicionar @synkra/aios-install
├── macos-testing.yml         # EXISTENTE — referência para cross-platform
├── bob-integration.yml       # CRIAR — testa bob-orchestrator em PRs
├── cross-platform.yml        # CRIAR — matrix test para pm.sh (3 OS)
├── pr-automation.yml         # EXISTENTE — não modificar
├── pr-labeling.yml           # EXISTENTE — não modificar
└── welcome.yml               # EXISTENTE — não modificar
```

[Source: `.github/workflows/`, padrão do projeto]

### Required Status Checks (para branch protection)

```yaml
required_status_checks:
  strict: true
  contexts:
    - lint           # de ci.yml
    - typecheck      # de ci.yml
    - test           # de ci.yml
    - bob-test       # de bob-integration.yml (NOVO)
```

[Source: Epic 12 — Story 12.11 Dev Notes]

### Source Tree Relevante

```
.github/
├── workflows/
│   ├── bob-integration.yml   # CRIAR — validação Bob em PRs
│   ├── cross-platform.yml    # CRIAR — matrix test pm.sh
│   ├── npm-publish.yml       # ATUALIZAR — @synkra/aios-install
│   └── ...                   # EXISTENTES — não modificar
└── README.md                 # CRIAR — documentação de workflows

.coderabbit.yaml              # CRIAR/ATUALIZAR — config de review automático
```

[Source: docs/architecture/source-tree.md, padrão do projeto]

### Tecnologias e Padrões

- **CI/CD:** GitHub Actions (YAML workflows)
- **Node.js:** v18+ (setup via `actions/setup-node@v4`)
- **Cache:** `actions/cache@v4` para npm dependencies
- **Path filter:** `dorny/paths-filter@v3` para change detection
- **npm publish:** `npm publish --access public` com `NPM_TOKEN` secret
- **Branch protection:** GitHub API via `gh api` ou Settings UI
- **Semantic versioning:** semantic-release com conventional commits
- **Concurrency:** `cancel-in-progress: false` para releases, `true` para CI

[Source: `.github/workflows/ci.yml`, GitHub Actions documentation]

### Testing

- **Framework:** GitHub Actions workflows (testados via PR de teste ou `act`)
- **Validação:** Verificar que cada workflow roda com sucesso
- **Dry-run:** npm publish com `--dry-run` para validar sem publicar
- **Branch protection:** Testar que merge é bloqueado sem status checks
- **Cross-platform:** Matrix com ubuntu-latest, macos-latest, windows-latest

[Source: docs/architecture/coding-standards.md #Testing Standards]

---

## PO Validation Notes

**Validated by:** @po (Pax)
**Date:** 2026-02-05
**Verdict:** ⚠️ CONDITIONAL APPROVAL (9/10)

### Conditions for Development

1. **Task 3 BLOQUEADA por Story 12.9** — A atualização do `npm-publish.yml` para publicar `@synkra/aios-install` depende de `packages/aios-install/` existir (Story 12.9 está em Draft, 0% progresso). **Tasks 1, 2, 4, 5, 6 podem iniciar imediatamente.** Task 3 deve ser deferida até 12.9 estar Done.

2. **Branch Protection (Task 5) deve ser a última** — Aplicar branch protection rules antes de merge das stories em andamento pode bloquear o fluxo de trabalho atual. Executar Task 5 somente após merge de stories pendentes no epic.

3. **Dependência circular 12.10 ↔ 12.11** — Story 12.10 AC11 (CI validation) foi deferido para esta story. Isso é aceitável e documentado. cross-platform.yml (Task 2) cobre essa lacuna.

4. **CodeRabbit GitHub App** — Verificar com @devops que a GitHub App do CodeRabbit está instalada no repositório antes de iniciar Task 4.

### Execution Order Recomendado

```
Task 1 (bob-integration.yml) → Task 2 (cross-platform.yml) → Task 4 (CodeRabbit)
→ Task 6 (release notes) → Task 7 (testes parciais) → Task 5 (branch protection)
→ [QUANDO 12.9 DONE] → Task 3 (npm-publish update) → Task 7 (testes completos) → Task 8 (integração final)
```

### Risks Identified

| # | Risco | Severidade | Mitigação |
|---|-------|-----------|-----------|
| R1 | 12.9 não iniciada — Task 3 bloqueada | HIGH | Desenvolver demais tasks primeiro; Task 3 deferida |
| R2 | Dependência circular 12.10 AC11 ↔ 12.11 | MEDIUM | Documentada e aceita |
| R3 | CodeRabbit App instalação | MEDIUM | Verificar previamente com @devops |
| R4 | Branch protection impacta PRs em andamento | MEDIUM | Aplicar por último |

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-05 | 1.0 | Story criada a partir do Epic 12 v1.6. Documenta 8 workflows existentes. Foco no delta: bob-integration.yml, cross-platform.yml, npm-publish update, branch protection, CodeRabbit config. | @sm (River) |
| 2026-02-05 | 1.1 | PO Validation: CONDITIONAL APPROVAL. Task 3 bloqueada por 12.9. Execution order definido. Risks documentados. | @po (Pax) |
| 2026-02-05 | 2.0 | Implementation: bob-integration.yml, cross-platform-bob.yml created. .coderabbit.yaml and .releaserc.json updated. Task 5 deferred. 316 tests passing. | @devops (Gage) |

---

## Dev Agent Record

### Agent Model Used
claude-opus-4-6 (via @devops Gage)

### Debug Log References
- Lint: 0 errors, 29 pre-existing warnings
- TypeCheck: pass (tsc --noEmit)
- Bob Core Tests: 125 passed (bob-orchestrator, terminal-spawner, lock-manager, workflow-executor-callbacks)
- Bob Supporting Tests: 191 passed (session-state, message-formatter, bob-status-writer, brownfield-handler, greenfield-handler)
- YAML Validation: All 10 workflow files parse correctly
- .releaserc.json: Valid, 6 plugins configured

### Completion Notes List
- bob-integration.yml: Includes 9 test files (story specified 4, expanded to cover all Bob modules for comprehensive CI)
- cross-platform-bob.yml: Named `cross-platform-bob.yml` instead of `cross-platform.yml` to avoid collision with existing cross-platform job in ci.yml
- workflow-executor.test.js does not exist — used `workflow-executor-callbacks.test.js` instead
- Node 20 used (matching ci.yml pattern) instead of Node 18 specified in story subtask 1.3
- npm-publish.yml already had @synkra/aios-install support from Story 12.9 — Task 3 mostly pre-completed
- .releaserc.json enhanced with conventionalcommits preset, changelog generation, and git commit-back
- Installed `conventional-changelog-conventionalcommits` devDependency for preset support
- Task 5 (branch protection) and AC5/AC9/AC10 DEFERRED per PO recommendation — apply after all epic stories merged
- Task 3.4 (environment protection rule) DEFERRED — requires GitHub Settings configuration post-merge

### Workflows Documentation

| Workflow | File | Trigger | Purpose |
|----------|------|---------|---------|
| CI | ci.yml | push main, PR main | Consolidated CI: lint, typecheck, tests, security audit |
| Bob Integration | bob-integration.yml | PR main (path filter) | Bob orchestrator tests + lint |
| Cross-Platform Bob | cross-platform-bob.yml | PR main (path filter) | pm.sh + terminal-spawner on 3 OS |
| NPM Publish | npm-publish.yml | release, tag v*.*.*, dispatch | Publish aios-core + @synkra/aios-install |
| Release | release.yml | tag v*, dispatch | Create GitHub Release + trigger npm-publish |
| Semantic Release | semantic-release.yml | dispatch only | Automated versioning via conventional commits |
| Security Test | test.yml | push main | npm audit + penetration test |
| PR Automation | pr-automation.yml | PR main | Auto-labeling, validation |
| PR Labeling | pr-labeling.yml | PR | Label management |
| Welcome | welcome.yml | issues, PR | Welcome new contributors |

### File List
| File | Status | Description |
|------|--------|-------------|
| `.github/workflows/bob-integration.yml` | CREATED | Bob orchestrator CI workflow |
| `.github/workflows/cross-platform-bob.yml` | CREATED | Cross-platform pm.sh/terminal-spawner tests |
| `.coderabbit.yaml` | MODIFIED | Added .aios-core/core/ and orchestration/ path instructions |
| `.releaserc.json` | MODIFIED | Enhanced with conventionalcommits preset, changelog, git plugins |
| `package-lock.json` | MODIFIED | Added conventional-changelog-conventionalcommits |
| `docs/stories/active/12.11.story.md` | MODIFIED | Task checkboxes, Dev Agent Record |

### Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-05 | 2.0 | Implementation: Tasks 1,2,3(partial),4,6,7,8 completed. Task 5 deferred per PO. | @devops (Gage) |
| 2026-02-05 | 2.1 | PO Review: Task 5 delegada a @devops. 5.1/5.3 marcados PRÉ-EXISTENTE. Delta: 5.2 (bob-test), 5.4 (squash merge). Executar pós-merge. | @po (Pax) |
